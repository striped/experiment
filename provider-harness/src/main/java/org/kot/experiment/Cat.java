package org.kot.experiment;

/**
 * The object interface. (Some proxy generator required it)
 */
public interface Cat {

	/**
	 * Method to be proxied.
	 * @return something, no really matter
	 * @param i
	 */
	int sayMeow(final int i);
}
